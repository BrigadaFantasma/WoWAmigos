<div id="splash-screen" class="fixed inset-0 flex items-center justify-center bg-[#0D0D0D]/95 z-50">
  <div class="relative flex flex-col items-center">
    <div class="relative">
      <svg width="100" height="100" viewBox="0 0 100 100" class="drop-shadow-[0_0_32px_#A52A2A] animate-pulse">
        <circle cx="50" cy="50" r="40" fill="#8B0000" filter="url(#glow)" />
        <text x="50%" y="55%" text-anchor="middle" fill="#EEEEEE" font-size="2.5rem" font-family="Orbitron, Arial, sans-serif" dy=".3em" font-weight="bold"
          style="text-shadow: 0 0 16px #A52A2A,0 1px 0 #fff4; background-size: 250%; background-position: center;">
          WA
        </text>
        <defs>
          <filter id="glow">
            <feGaussianBlur stdDeviation="6" result="coloredBlur"/>
            <feMerge>
              <feMergeNode in="coloredBlur"/>
              <feMergeNode in="SourceGraphic"/>
            </feMerge>
          </filter>
        </defs>
      </svg>
      <!-- Partículas con tonos rojos y negros -->
      <div class="absolute inset-0 pointer-events-none">
        <div class="animate-ping absolute w-3 h-3 bg-[#FF4444] rounded-full left-1/4 top-1/4 opacity-80"></div>
        <div class="animate-ping absolute w-2 h-2 bg-[#0D0D0D] rounded-full left-2/3 top-1/3 opacity-70"></div>
        <div class="animate-ping absolute w-1.5 h-1.5 bg-[#B22222] rounded-full left-1/2 top-2/3 opacity-60"></div>
        <div class="animate-ping absolute w-2 h-2 bg-[#A52A2A] rounded-full left-3/4 top-1/2 opacity-90"></div>
      </div>
    </div>
    <h1 class="mt-6 text-3xl md:text-4xl font-extrabold tracking-wider text-transparent bg-clip-text bg-gradient-to-r from-[#FF4444] via-[#FFC0CB] to-[#EEEEEE] drop-shadow-[0_2px_16px_#A52A2A] leading-normal animate-pulse letras-futurista">
      WoWAmigos
    </h1>
  </div>
</div>

<script>
window.addEventListener("load", () => {
  setTimeout(() => {
    const splash = document.getElementById("splash-screen");
    if (splash) {
      splash.style.opacity = "0";
      setTimeout(() => {
        splash.style.display = "none";
      }, 500);
    }
  }, 2000);
});
</script>
